propel:
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=localhost;dbname=pet4web
                user: root
                password: 
                settings:
                    charset: utf8
