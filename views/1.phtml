<select id="foo">
        <option value="">Sort by:</option>
        <option value="<?php echo URL?>petition/index&q=name">Name</option>
    </select>

    <script>
        document.getElementById("foo").onchange = function() {
            if (this.selectedIndex!==0) {
                window.location.href = this.value;
            }
        };
    </script>